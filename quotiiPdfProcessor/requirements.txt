# Direct dependencies only — transitive packages are resolved by pip.
# App / HTTP
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
httpx>=0.27,<1

# Config / DB / object storage
python-dotenv>=1.0.1
pymongo[srv]>=4.8.0,<5
boto3>=1.35.0,<2

# Queue / background worker (arq pulls redis; pin <5.2 — redis 5.2+ hard-imports jwt)
arq>=0.25,<1
redis>=4.2,<5.2

# PDF extract / OCR
pdf-inspector==0.2.6
docling==2.69.1
